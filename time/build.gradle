plugins {
    id 'java'
}

group 'com.strixmc.acid'
version '1.0.0'

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.strixmc.acid'
            artifactId = 'time'
            version = '1.0.0'

            from components.java
        }
    }
}